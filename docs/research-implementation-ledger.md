# NotebookLM Research → NovaTools Implementation Ledger

Central, living record tracing every requirement pulled from the research
corpus (`C:\Users\MeteHUN48\Desktop\NotebookLLM çalışmaları\WEB SİTE TASARIM\`)
to its actual disposition in this repo. Updated incrementally as work
proceeds — never rewritten from scratch. Statuses used:
`IMPLEMENTED`, `ALREADY_EXISTS`, `IMPROVED`, `READY_NEXT`, `WATCHLIST`,
`NOT_APPLICABLE`, `REJECTED_WITH_REASON`, `BLOCKED_EXTERNAL`.

## Source files inventoried so far

| File | Type | Status |
|---|---|---|
| `notebookllmweb.txt` | txt | Parsed (master GEO/SEO/architecture research doc) |
| `nova tools düzeltme.txt` | txt | Parsed through line ~1030 (forensic analysis, FSD/TS strategy guides, master plans) |
| `2027+ Web Sitesi Teknik Terim ve Konu Raporu.txt` | txt | Section 1 (technology glossary) scanned; bulk-classified rather than read term-by-term (see REQ-014) |
| `apiKeyService.ts`, `auditEngine.ts`, `schemaParser.ts`, `geo-inspector-*.tsx`, `b2b-scan-route.ts`, `openapi-spec.ts`, `security-scan.yml`, `ci-cd.yml`, `docker-compose.yml`, `playwright.config.ts`, `robots.ts`, `sitemap.ts`, `white-label-settings-page.tsx` | Next.js/Prisma/Docker code samples | Reviewed as *design reference only* — see REQ-001 |
| PDF (`ACFrOgB...pdf`) | pdf | Not yet read |

## Requirements

| REQ_ID | Source | Requirement | Category | Priority | Current State | Decision | Files | Test | Status | PR/Evidence |
|---|---|---|---|---|---|---|---|---|---|---|
| REQ-001 | notebookllmweb.txt | Build "AI SEO/GEO Compliance Inspector" tool | PRODUCT | P0 | Did not exist | Implemented, adapted to actual Vite/Vercel-Cloudflare stack (not the Next.js/Prisma samples, which assumed a different framework entirely) | `src/tools/dev/geo-compliance-inspector/*`, `api/geo-scan.js` | `tests/geo-compliance-inspector.mjs` (25 tests) | **IMPLEMENTED** | PR #239 (merged) |
| REQ-002 | Security review (automated) | GEO scan endpoint SSRF hardening (DNS rebinding, redirect bypass, cache poisoning) | SECURITY | P0 | Initial version vulnerable to redirect-follow bypass + hostname-only allowlist | Fixed: manual redirect validation (max 3 hops), DNS-resolved-address validation (`ip-guard.mjs`), IP-pinned fetch on Node runtime, `private, no-store` cache-control | `api/geo-scan.js`, `src/tools/dev/geo-compliance-inspector/ip-guard.mjs` | `tests/geo-compliance-inspector.mjs` | **IMPLEMENTED** | PR #239 (merged) |
| REQ-003 | Direct verification (re-derived from re-checking claimed prod architecture) | Production is served by Cloudflare Pages, not Vercel; Vercel is preview-only | ARCHITECTURE | P0 | Confirmed live via response headers (`server: cloudflare`) and `vercel.json`'s `deploymentEnabled.main: false` | Documented; drives REQ-004 | — | — | **ALREADY_EXISTS** (discovered, not created) | — |
| REQ-004 | Direct verification | `api/geo-scan.js` and `api/live-data.js` (Vercel-only convention) never reach production (Cloudflare reads `functions/`, not `api/`) | ARCHITECTURE / P0 BUG | P0 | Confirmed live: `curl mc-novatools.com/api/live-data?resource=crypto` → 404. 3 already-CERTIFIED finance tools (live-exchange, crypto-prices, stock-lookup) had a non-functional backend in production | Ported both endpoints to `functions/api/*.js`; extracted `live-data.js` into a runtime-agnostic shared module (`src/server/control-plane/live-data.mjs`), matching the existing `entitlements.mjs` pattern | `functions/api/geo-scan.js`, `functions/api/live-data.js`, `src/server/control-plane/live-data.mjs`, `wrangler.toml` (`nodejs_compat`) | Verified live via `wrangler pages dev` against real HTTP requests incl. SSRF payloads | **IMPLEMENTED** | PR #246 (open) |
| REQ-005 | `nova tools düzeltme.txt` | "Remove vercel.json, make Cloudflare Pages the sole deployment surface" | ARCHITECTURE | — | Vercel already explicitly disabled for `main` (`deploymentEnabled.main: false`); only produces PR previews | **REJECTED_WITH_REASON**: removing it loses PR-preview QA for zero production benefit — the actual bug (REQ-004) was the `api/` vs `functions/` split, not vercel.json's mere existence | — | — | **REJECTED_WITH_REASON** | — |
| REQ-006 | `nova tools düzeltme.txt` | Migrate to Feature-Sliced Design + strict TypeScript | ARCHITECTURE | — | Repo is a mature, working Vite vanilla-JS MPA with 130+ certified tools and extensive existing automation (audit scripts, certification matrix, CI gates) | **REJECTED_WITH_REASON**: this is generic "how to build a new SaaS" boilerplate advice, not NovaTools-specific analysis. A full FSD/TS-strict rewrite is exactly the "big-bang rewrite with no measurable benefit" the operating instructions themselves forbid. No current architecture limitation this would solve that isn't already solved | — | — | **REJECTED_WITH_REASON** | — |
| REQ-007 | `nova tools düzeltme.txt` | `ads.txt` must be UTF-8 BOM-free, correctly formatted | SEO/ADSENSE | P0 (claimed) | Checked via `xxd` — no BOM, single correct line (`google.com, pub-..., DIRECT, ...`) | No action needed | `public/ads.txt` | Manual `xxd` inspection | **ALREADY_EXISTS** | — |
| REQ-008 | `nova tools düzeltme.txt` | `robots.txt` must not block crawlers as "suspicious traffic" | SEO | P0 (claimed) | Inspected — allows `/`, correctly scoped disallows (`/api/`, `/admin/`, `/test/`, stray query params), lists all 3 sitemaps | No action needed | `public/robots.txt` | Manual inspection | **ALREADY_EXISTS** | — |
| REQ-009 | `nova tools düzeltme.txt` | Sitemap must respect the 50,000 URL / 50MB limit, use a sitemap index if exceeded | SEO | P1 (claimed) | 164 URLs total across all 3 sitemap files — nowhere near the limit | No action needed now; revisit if tool/blog count grows an order of magnitude | `public/sitemap*.xml` | `grep -c "<loc>"` | **NOT_APPLICABLE** (at current scale) | — |
| REQ-010 | `nova tools düzeltme.txt` | Google Consent Mode v2 must be "fully automated" for AdSense | ADSENSE / PRIVACY | P0/P1 (claimed) | A real, working custom consent banner (`src/core/consent-manager.mjs`) exists for analytics/functional categories. It explicitly defers *advertising* consent to Google's own certified Funding Choices message — but `fundingchoicesmessages.google.com` was missing from the CSP allowlist entirely, so that message could never load regardless of AdSense dashboard config | Added the domain to `script-src`/`connect-src`/`frame-src` | `scripts/harden-csp.mjs`, `vercel.json` | Rebuild + CSP generation check | **IMPROVED** | PR #250 (open) |
| REQ-011 | `nova tools düzeltme.txt` | Whether AdSense's own dashboard "Consent Mode"/"Funding Choices" toggle is turned on | ADSENSE | P0/P1 | Cannot be checked from the repo or a logged-out browser — this is an AdSense account setting, not a code artifact | Not fixable in code | — | — | **BLOCKED_EXTERNAL** — requires the site owner to check AdSense dashboard → Privacy & Messaging → European regulations | — |
| REQ-012 | Multiple docs (repeated across nearly every research file) | Playwright visual-regression snapshots must be generated in Linux CI, not locally, to avoid OS font-rendering false positives | TESTING/CI | P2 | Not yet verified against actual `playwright.config.mjs`/CI workflow | To investigate next | `playwright.config.mjs`, `.github/workflows/*` | — | **READY_NEXT** | — |
| REQ-013 | Multiple docs | Google Consent Mode v2 `gtag('consent', 'default', {...})` explicit denial-by-default block for analytics | ADSENSE/PRIVACY | P1 | The custom banner already keeps `analytics_storage`-equivalent gating inert-by-default via the `data-consent-category` script pattern (functionally equivalent, not using literal `gtag('consent', ...)` calls) | Functionally covered by the existing custom mechanism; not a gap | `src/core/consent-manager.mjs`, `src/components/Analytics.mjs` | — | **ALREADY_EXISTS** (different mechanism, same effect) | — |
| REQ-014 | `2027+ Web Sitesi Teknik Terim ve Konu Raporu.txt` §1 | ~150 web platform APIs (WebGPU, WebXR, WebUSB, Web Bluetooth, Web NFC, WebHID, CSS Houdini, Worklets, Temporal API, etc.) | FUTURE WEB | — | This is a comprehensive technology glossary, not a set of NovaTools-specific requirements | Bulk-classified: **WATCHLIST** for anything with plausible future relevance to a browser-based free-tools site (WebGPU for future compute-heavy tools, File System Access API for local file tools); **NOT_APPLICABLE** for the rest (WebXR, Web Bluetooth/USB/NFC/HID, CSS Houdini paint worklets — no plausible fit for this product). Individual line-by-line research was judged disproportionate effort for near-zero decision value; a technology only gets individually evaluated when a specific tool idea would need it | — | — | **WATCHLIST / NOT_APPLICABLE** (bulk) | — |
| REQ-015 | `2027+ ...Raporu.txt` §2 | Frontend framework glossary (Next.js, Remix, Astro, Svelte, Vue, Angular, SolidJS, Qwik, Blazor, Leptos/Yew/Dioxus (Rust)) | FUTURE WEB | — | Same as REQ-014 — generic glossary | **NOT_APPLICABLE**: current Vite MPA architecture is deliberately framework-light and works well for 130+ independent tool pages; no measurable benefit case for any of these per the "no big-bang rewrite" rule (see REQ-006) | — | — | **NOT_APPLICABLE** | — |

## Next up (P1, in progress)

- Tool registry / source-truth spot-check: verify a sample of certified tools' routes actually work end-to-end (route → UI → real functionality → category listing → sitemap presence), per instruction section on P1.
- REQ-012 (Playwright snapshot CI location).
- Continue reading remaining unread research material (§3+ of the terms report, the PDF) for any further NovaTools-specific — not generic — findings.
