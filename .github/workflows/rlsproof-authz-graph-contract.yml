name: RLSProof Authorization Graph Contract

on:
  pull_request:
    paths:
      - 'src/tools/security/rlsproof/**'
      - 'tests/rlsproof-authz-graph.mjs'
      - '.github/workflows/rlsproof-authz-graph-contract.yml'

permissions:
  contents: read

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run RLSProof authorization graph contract
        run: node tests/rlsproof-authz-graph.mjs
